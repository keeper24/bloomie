# Assets

Drop demo media here so the main README can reference it:

- `login.png`, `home.png`, `diagnostic.png`, `result.png` — screenshots used in the
  README screens table (390px-wide mobile captures look best).
- `demo.mp4` (optional) — the walkthrough recording, if you host it in-repo instead
  of drag-and-dropping it into the README on GitHub.
